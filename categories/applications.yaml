apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: applications-helm
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/jeremy-misola/GitOps-Homelab
      revision: develop
      files:
        - path: argocd-apps/apps/**/*-helm-app.yaml
  template:
    metadata:
      name: '{{name}}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{syncWave}}'
    spec:
      project: default
      source:
        repoURL: '{{repoURL}}'
        chart: '{{chart}}'
        targetRevision: '{{targetRevision}}'
        helm:
          values: |
            {{values}}
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
      # Applications depend on all infrastructure apps being healthy first
      ignoreDifferences:
        - group: argoproj.io
          kind: ApplicationSet
          jsonPointers:
            - /status
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: applications-git-helm
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/jeremy-misola/GitOps-Homelab
      revision: develop
      files:
        - path: argocd-apps/apps/**/*-git-helm-app.yaml
  template:
    metadata:
      name: '{{name}}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{syncWave}}'
    spec:
      project: default
      source:
        repoURL: '{{repoURL}}'
        targetRevision: '{{targetRevision}}'
        path: '{{appPath}}'
        helm:
          values: |
            {{values}}
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
      ignoreDifferences:
        - group: argoproj.io
          kind: ApplicationSet
          jsonPointers:
            - /status
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: applications-git
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/jeremy-misola/GitOps-Homelab
      revision: develop
      files:
        - path: argocd-apps/apps/**/*-git-app.yaml
  template:
    metadata:
      name: '{{name}}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{syncWave}}'
    spec:
      project: default
      source:
        repoURL: '{{repoURL}}'
        targetRevision: '{{targetRevision}}'
        path: '{{appPath}}'
        directory:
          recurse: true
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
      # Applications depend on all infrastructure apps being healthy first
      ignoreDifferences:
        - group: argoproj.io
          kind: ApplicationSet
          jsonPointers:
            - /status
