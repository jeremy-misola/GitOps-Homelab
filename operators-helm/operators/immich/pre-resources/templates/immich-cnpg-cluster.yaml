apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  imageName: ghcr.io/tensorchord/cloudnative-vectorchord:14-v0.0.2
  instances: {{ .Values.cnpgInstances }}
  storage:
    size: {{ .Values.cnpgStorage }}
    storageClass: longhorn
  enableSuperuserAccess: true
  bootstrap:
    initdb:
      database: immich
      owner: immich
      secret:
        name: immich-db-credentials
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi