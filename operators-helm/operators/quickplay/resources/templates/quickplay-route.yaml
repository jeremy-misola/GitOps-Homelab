apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: quickplay-route
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
  - name: {{ .Values.gatewayName }}
    namespace: envoy-gateway-system
  hostnames:
    - quickplay{{ .Values.envSuffix }}.jeremymr.dev
  rules:
  - backendRefs:
    - name: quickplay
      port: 8080
