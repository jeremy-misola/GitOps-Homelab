fullnameOverride: navidrome

env:
  TZ: America/Toronto

persistence:
  config:
    enabled: true
    storageClass: local-path
    accessMode: ReadWriteOnce
    size: 2Gi
  music:
    enabled: true
    storageClass: local-path
    accessMode: ReadWriteOnce
    size: 50Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1Gi
