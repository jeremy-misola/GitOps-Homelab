apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: navidrome-route
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
  - name: {{ .Values.gatewayName }}
    namespace: envoy-gateway-system
  hostnames:
    - navidrome{{ .Values.envSuffix }}.jeremymr.dev
  rules:
  - backendRefs:
    - name: navidrome
      port: 4533
