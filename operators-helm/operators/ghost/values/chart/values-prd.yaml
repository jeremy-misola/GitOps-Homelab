global:
  fullnameOverride: ghost

controllers:
  main:
    containers:
      main:
        image:
          repository: ghost
          # Current official themes (Casper/Source/Edition/etc) require the
          # `social_accounts` helper, only present in Ghost 6+. No content
          # exists yet -- just the owner account -- so upgrading in place
          # from the old 5.x line is low-risk.
          tag: 6.63.0-alpine
        env:
          NODE_ENV: production
          url: https://blog.jeremymr.dev
          # SQLite instead of MySQL -- single container, single PVC, no extra
          # StatefulSet/credentials to operate. Fine for a personal blog;
          # revisit if traffic or write concurrency ever becomes a problem.
          database__client: sqlite3
          database__connection__filename: /var/lib/ghost/content/data/ghost.db
          database__useNullAsDefault: "true"

service:
  main:
    controller: main
    ports:
      http:
        port: 2368

persistence:
  content:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    storageClass: local-path
    size: 5Gi
    globalMounts:
      - path: /var/lib/ghost/content

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1Gi
