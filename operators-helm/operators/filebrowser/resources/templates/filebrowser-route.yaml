apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: filebrowser-route
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
  - name: {{ .Values.gatewayName }}
    namespace: envoy-gateway-system
  hostnames:
    - navidrome-upload{{ .Values.envSuffix }}.jeremymr.dev
  rules:
  - backendRefs:
    - name: filebrowser
      port: 80
