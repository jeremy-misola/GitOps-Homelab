apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: slskd-route
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
  - name: {{ .Values.gatewayName }}
    namespace: envoy-gateway-system
  hostnames:
    - slskd{{ .Values.envSuffix }}.jeremymr.dev
  rules:
  - backendRefs:
    - name: slskd
      port: 5030
