apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: excalidraw-tailscale-ingress
  namespace: excalidraw
spec:
  defaultBackend:
    service:
      name: excalidraw-main
      port:
        number: 80
  tls:
    - hosts:
        - excalidraw
  ingressClassName: tailscale
