apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-tailscale-ingress
  namespace:
spec:
  defaultBackend:
    service:
      name: longhorn-frontend
      port:
        number: 80
  tls:
    - hosts:
        - longhorn
  ingressClassName: tailscale
