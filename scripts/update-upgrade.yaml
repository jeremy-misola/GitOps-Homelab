---
- hosts: all
  become: true
  tasks:
    - name: Update apt cache and upgrade all installed packages
      ansible.builtin.apt:
        update_cache: yes
        upgrade: yes
        cache_valid_time: 3600