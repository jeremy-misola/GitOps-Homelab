apiVersion: apps/v1
kind: Deployment
metadata:
  name: kleff-frontend
  namespace: kleff-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kleff-frontend
  template:
    metadata:
      labels:
        app: kleff-frontend
        paas: kleff
    spec:
      imagePullSecrets:
        - name: acr-creds
      containers:
        - name: kleff-frontend
          image: kleff.azurecr.io/frontend:77364a9dc578579d74d63bd8eab5ff8ebf7a446d
          env:
            - name: VITE_AUTH_CLIENT_ID
              value: "8qNA2sSV88fusOocZcMj6GirTLWYygdAXnmtLUfe"
            - name: VITE_BACKEND_URL
              value: https://api.kleff.io
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: kleff-frontend
  namespace: kleff-deployment
spec:
  selector:
    app: kleff-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
