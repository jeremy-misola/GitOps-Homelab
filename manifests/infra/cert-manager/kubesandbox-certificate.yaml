apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubesandbox-cert
  namespace: envoy-gateway-system
spec:
  secretName: kubesandbox-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "kubesandbox.com"
  - "*.kubesandbox.com"